info:
  name: Revenue
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/reports/revenue"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
