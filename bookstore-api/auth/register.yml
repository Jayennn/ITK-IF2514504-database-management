info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "anton",
        "password": "12345678",
        "confirmPassword": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
