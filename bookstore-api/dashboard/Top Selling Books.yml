info:
  name: Top Selling Books
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/reports/top-selling"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
