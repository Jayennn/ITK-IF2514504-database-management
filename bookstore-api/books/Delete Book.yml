info:
  name: Delete Book
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:3000/api/v1/books/9
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
