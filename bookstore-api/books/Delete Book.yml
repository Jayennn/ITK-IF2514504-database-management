info:
  name: Delete Book
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/books/:bookId"
  params:
    - name: bookId
      value: "11"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
