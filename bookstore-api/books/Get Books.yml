info:
  name: Get Books
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/books"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
