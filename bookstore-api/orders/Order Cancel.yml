info:
  name: Order Cancel
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/orders/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
