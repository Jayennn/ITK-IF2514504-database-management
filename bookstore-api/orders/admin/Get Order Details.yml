info:
  name: Get Order Details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/orders/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
