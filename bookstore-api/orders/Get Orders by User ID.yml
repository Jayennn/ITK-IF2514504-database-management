info:
  name: Get Orders by User ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/orders/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
